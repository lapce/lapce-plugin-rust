[package]
name = "lapce-plugin"
version = "0.1.0"
license = "Apache-2.0"
description = "rust libary for Lapce plugin"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0.59"
